<h1>Order creation</h1>

<form action="{{ path('test_order_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('test_order') }}">
            Back to the list
        </a>
    </li>
</ul>
